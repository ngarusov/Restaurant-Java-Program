# Restaurant-Java-Program
## Properties
## Variables set up 
- int waitTime
- double rating
- double avgPrice
- double distance
- String hoursOpen
  
- inputDialog, which basically brings a pop-up window that will accept an input 
- Getters and Setters
- Arraylist
- toString()

March 22
- We made it so it only prints the name and contents of one restaurant, this is done by creating a list. Each restaurant in the list has variables that we set up
-- int waitTime
-- double rating
-- double avgPrice
-- double distance
-- String hoursOpen

- We also set it up so that the restaurants that are printed out are chosen at random 
  - this is done by using the random class, which as you would guess picks out a random number and that random number is the postion of the chosen printed restaurant in the list.


